spring:
  application:
    name: portfolio-backend-main
  ai:
    deepseek:
      api-key: ${DEEPSEEK_API_KEY}
      chat:
        model: deepseek-v4-flash
        max-tokens: 350
server:
  port: ${PORT:8080}