spring:
  application:
    name: portfolio-backend-main
  ai:
    deepseek:
      api-key: ${DEEPSEEK_API_KEY}
      chat:
        model: deepseek-v4-flash
        max-tokens: 600
server:
  port: 8080
